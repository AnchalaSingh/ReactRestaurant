import React from "react";
import { data } from "../restApi.json";
const Team = () => {
  return (
    <section className="team" id="team">
      <div className="container">
        <div className="heading_section">
          <h1 className="heading">OUR TEAM</h1>
          <p>
          Meet the passionate team at Lazzez Foods, where culinary artistry and warm hospitality converge.
          From our accomplished chefs to our dedicated service staff, every member is committed to delivering exceptional dining experiences.
          Our team's diversity, professionalism, and shared passion for hospitality create a welcoming and memorable atmosphere at Lazzez Foods.
          </p>
        </div>
        <div className="team_container">
          {data[0].team.map((element) => {
            return (
              <div className="card" key={element.id}>
                <img src={element.image} alt={element.name} />
                <h3>{element.name}</h3>
                <p>{element.designation}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
};

export default Team;
